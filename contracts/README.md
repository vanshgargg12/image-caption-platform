# Contracts

OpenAPI specifications and shared JSON schemas for the Image Caption Platform will
live here.

## Planned Contents

- Public backend API (`/api/v1/*`) OpenAPI document
- Shared error response schema with stable machine-readable codes
- Request/response models for caption generation

## Current Status

No OpenAPI files yet. The backend currently exposes only `GET /api/v1/health` as a
scaffolding endpoint.
